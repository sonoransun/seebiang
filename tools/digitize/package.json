{
  "name": "seebiang-digitize",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based stroke tracer for See Biáng! — loads a reference SVG and emits per-stroke character JSON.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
