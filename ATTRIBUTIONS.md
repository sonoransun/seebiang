# Attributions

This file aggregates per-variant provenance. Each character JSON carries its
own `source` block; this manifest summarises them.

## Format

Each entry links the character `id`, the source kind, the upstream URL (if
any), the author/attribution string, and the licence.

## Entries

| id | source.kind | upstream | author / attribution | licence |
|---|---|---|---|---|
| toy-trio | hand-crafted | — | See Biáng! project | CC0-1.0 |

<!-- Append new rows as variants are digitised. A pre-commit check will
validate that every JSON under data/characters/ has a row here. -->
